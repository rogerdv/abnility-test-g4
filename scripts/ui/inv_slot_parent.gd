extends TextureRect

#the top elemnt in the inventory button
#Does not handles drag and drop

var inv_window
var inv_item_index	#what array index holds the item?

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
